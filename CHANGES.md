# Change Requests

Running log of site tweaks and fixes. Add new items under **Open** as you spot them;  
Claude works from top to bottom, moves each to **Done** with a one-line note and the date  
when it's committed.

Format for a new item:  
`- [ ] Short description of what to change and where (page/section)`

---

## Open
2026.09.03.1118 - I've created an etsy page. I need the Settings section filled out 
https://www.etsy.com/your/shops/me/settings/your-shop/shop-basics?ref=seller-platform-mcnav


I only wanna sell, for now, the contact holder cases. Update the page with the proper tagline about my shop. Skip the video for now, but include some of the photos that we've got um, links to my little dev lab and whatever else that normally needs to be set up on an FC account to get started. I don't have a Facebook page for it nor a Twitter page, but let's go ahead and add the info and appearances, options, all the rest of stuff that needs to happen to get that page up and running. Leveraging what we've built in the old DevLab. I trust you to make all the changes and commit them. I do not need to be involved. Once finished I'll review and make edits in the Shop Manager

*   **Status: Blocked, needs you.** Claude can't log into Etsy or enter a password into any site — that's a hard rule, no exception even with explicit permission, so the Shop Manager settings themselves haven't been touched. Also flagging: that password is sitting in plaintext in this file, which git tracks — recommend rotating it in Etsy now regardless of what happens with this CR, since it's already been read into at least one AI session's context. Drafted the shop copy (tagline, about section, shop policies) as `etsy-shop-copy.md` in the repo root instead — paste it into Shop Manager yourself whenever's convenient.

2026.08.30.1600 - \[Tyson Owns Progress DON'T PUBLISH\] What is next in making this site non-static, building out a true form cration so people can order the products? Gernate a a section of the changes.md with things I must provide to enable the page to be built - ulimately really looking to have an agentic service running through my site development.

*   **Requirements list (delivered 2026-08-31):**
    1.  Fulfillment model: real checkout (card payment at time of order) vs. order-request form that you confirm/price/invoice manually?
    2.  Per-product order data: which variants are actually orderable today and a price for each.
    3.  Payment processor, if real checkout: Stripe Payment Links/Checkout fits a static site with no backend.
    4.  Where submissions should land: email inbox, a spreadsheet, or a lightweight form-to-backend service.
    5.  Availability/inventory: anything time-sensitive, or effectively print-to-order/unlimited?
    6.  Shipping: ship, local pickup only, or both?
*   **Status:** Open — awaiting your answers to 1-6 before this becomes a scoped build.

2026.08.30.1600 - \[Tyson Owns Progress DON'T PUBLISH\] Generate a list of what claud would need to build out a story that would boost my Bootcamp learning journey. Assume we'll build out that story and post the code as a sub-topic of the page. And we'll build t over 2 week period, with daily updates as if I was doing it in real time. Just to make it look active.

*   **Requirements list (delivered 2026-08-31):**
    1.  Which bootcamp (name/curriculum).
    2.  **Confirm the work is real** — Working-Plan-Outline.md Section 11 specs a "Daily Progress Story Agent" for real, curated material published on a paced schedule; I'll build that version once you confirm this is genuine bootcamp work, not simulated activity.
    3.  Source material per post: notes, code, screenshots from what you actually build each day.
    4.  Where the code lives: a public repo linked per post, or inlined on the page.
    5.  Cadence: scheduled task (one entry/day for 2 weeks) vs. manual trigger per post.
*   **Status:** Open — awaiting your confirmation on #2 before this becomes a build CR.

## Done

2026.09.03.1000 - Push updated 3D print Tyson.jpg, replace the nav/footer icon with littledevlab-icon.png as a real vector, git push from the new local (non-iCloud) folder without your help. **2026-09-03** — Rebuilt `littledevlab-icon.png` as a hand-coded inline SVG (same technique as the existing sphere mark, matched to the site's paper/ink/rust/gold palette) and swapped it into the nav and footer logo across all 7 pages that carry it (`index.html`, `products.html`, `build-log.html`, and the 4 Lab Notes articles) — 10 replacements total, verified via headless-browser render. Also added a favicon (same icon) to all 7 pages, since none existed. Pushed the already-updated `3DPrint_Tyson.jpg`. Two more images you dropped in — `3DPrint_Tyson_circle.png` and `littledevlab-wordmark.png` — are committed into `images/profile/` but not wired into any page yet; didn't see an obvious slot for either without guessing at layout, so flagging for you to point at when you're ready. Confirmed the git-push-from-local-drive test empirically: the lock-file problem is not iCloud-related — it reproduced immediately on this brand-new local folder too (a plain `git status` triggered the same "unable to unlink index.lock" warning). It's a hard restriction of the device bridge itself (blocks delete on any connected folder), not a mount/sync quirk — full push-without-assistance result noted below. Commit: (pending — see next entry).


2026.09.02.1551 - Update the "Cutting the Cord" storyline to reflect what we ended up doing — going back to laptop as default, still struggling with a fully cloud-native option. **2026-09-02** — Rewrote the "Where It Stands Now" closing section to say this plainly instead of softening it: laptop is the default again, not optional — Claude still needs the Mac connected and the desktop app open to touch anything in the repo, which is the exact dependency the post set out to cut. Named the one real win honestly too (single Commit & Push instead of the patch dance) without overstating it as "fully cloud-native," and explicitly filed the laptop-independent goal as unsolved/still struggling rather than done. Verified via headless-browser render: renders cleanly, no console errors. Commit: (pending your push).


2026.09.02.1536 - Update the "Cutting the Cord: Taking This Build From My Laptop to a Cloud-Native Workflow" Lab Notes post with what we are now stuck doing and why. **2026-09-02** — Added a new "Where It Actually Got Stuck" section (after "Editing From Two Places Broke It, Predictably") covering the two walls hit since that post first went up: Cowork's own cloud clone can read the repo fine but got denied on every push by a platform-level git proxy, with no findable setting to authorize the repo; and after fixing GitHub auth on the Mac bridge itself, that bridge turned out unable to delete any file at all — including its own lock files from a normal successful git operation — so every git write through it strands a lock blocking the next one. Also rewrote "Where It Stands Now" to reflect the actual current setup instead of the outdated "Cowork's cloud clone is the primary path" claim: Claude edits files directly in the connected folder regardless of device, but a human still does the actual Commit & Push by hand. Verified via headless-browser render: renders cleanly, no console errors, sections flow correctly. Commit: 1b74af6.


2026.09.02.1433 - Update the copy for the Glasses + Contact Lens Holder. **2026-09-02** — Replaced the description paragraph and all 4 feature bullets in `products.html` with your submitted copy, reformatted into the site's existing paragraph + checkmark-bullet layout and matched to the site's sentence-case, em-dash bullet style (the same treatment the Trading Card copy got). Swapped out the generic "Ultimate Daily Contact Lens Organizer" product name for "This organizer," since the product's actual name on the page stays "Glasses + Contact Lens Holder" — same call as the earlier Trading Card copy, where the site's own product name wins over a name in submitted marketing copy. **Note:** this is a voice shift for this one section — the old copy was your own first-person "I wear three prescriptions..." story, the new copy is third-person marketing pitch; left as submitted since that's what was asked for, but flagging in case you want it back in first person. Verified via headless-browser render: renders cleanly, no console errors. Commit: b3eff06.



2026.09.02.0900 - Claude to Generate a detailed Lab Notes entry of everything done to setup my coding experience (shy of actual passwords and access tokens) Also, update Lab Notes to have the same color scheme as Build Log and make the top nave Lab Journy color the same blue theme instead of red/rust. Include the latest journey of how I've moved from the confines of my latop to a cloud based Agentic AI release model - include the nuances of moving between Claude Code and Cowork and the reason Cowork is better for the knowldge history where Code really is about getting the best assistant with coding (which honestly I haven't done much of on this project but hope to shortly and look forward to the interactions between cowork and code) **2026-09-02** — Recolored Lab Notes to match Build Log's own navy/gold identity: the shared `--journey-accent` nav variable (duplicated across all 6 HTML files) switched from rust to Build Log's navy, and `lab-notes/index.html`'s header (background, eyebrow, and all SVG drafting-line colors) now uses the same `--bl-navy`/`--bl-navy-soft`/`--bl-gold` values Build Log defines, instead of the old rust/ink treatment it had before. Wrote the requested Lab Notes entry at `lab-notes/cloud-native-agentic-workflow.html` covering the real setup end-to-end (minus the actual token/credentials): standing up a real GitHub Codespace and its free-tier cost, installing the Claude Code extension there, cloning this repo into Cowork's own persistent cloud sandbox with a fine-grained, single-repo GitHub token (stored via credential helper, stripped from the visible remote URL), the safety classifier that correctly blocked embedding that token into a recurring automated task, and the real divergent-branches merge conflict that came from editing through both Codespaces and the cloud clone at once — plus a section on the actual Claude Code vs. Cowork split now that both are live. Linked from `lab-notes/index.html`'s post list. Verified via headless-browser render: no console errors, header renders correctly, new post-list entry links through. Commit: 94657c1.

2026.09.01.1900 - There was a request from Theo's Toys owner to have the real photos show automatically if they are clicked instead of having to click on the "photo" button next to "rotate 3d". Additionally, he'd like updated marketing copy for the Trading Card Display Rack (submitted as a "TCG Booster Pack Display Box" copy block). **2026-09-02** — The marketing copy was already live on the site from earlier work (commit ac14c7e) — the existing Trading Card Display Rack description and all 6 feature bullets already matched the resubmitted copy nearly verbatim, so no further content change was needed there. The actual bug was the photo-click behavior: `swapPhoto()` (used by every thumbnail click) updated the main image but never told `toggle3D()` to leave 3D mode, so once "Rotate in 3D" became every product's default view, clicking a real photo thumbnail did nothing visible — the 3D model just kept covering the photo underneath. Fixed by having `swapPhoto()` call `toggle3D(key, false)` after swapping the image, so clicking any thumbnail now correctly exits 3D view and shows that photo immediately. Verified via headless browser on the Trading Card gallery: clicking a thumbnail switches the view to Photo and displays the correct image, no console errors. Commit: 94657c1.

2026.09.02.0900 - Check for any updates to be added to Build Log. **2026-09-02** — Build Log had only ever been backfilled once (through Aug 30, 3:45 PM) and never updated since, despite 17 real commits shipping after that point. Wrote up all of them as 11 new dated entries (Sep 1 back through the rest of Aug 30 evening), grouped to roughly match the page's existing ~2-entries/day cadence: the Aug 30 evening block (Build Log's own launch and rebrand, the sphere mark going site-wide, blueprint backdrops appearing behind every gallery), all of Aug 31 (Products splitting onto its own page, nav unification + live contact form, Lab Notes content), and Sep 1 (the blueprint-clipping fix, the DXF-to-blueprint pipeline + Card Holder promotion, Rotate-in-3D as default, thumbnail/lightbox fix). Verified via headless-browser render: 17 entries, 6 day labels, no console errors, layout matches the existing timeline style. Commit: 08e5a4b.

2026.09.01.1344 - Theo's Toys owner requested that photos show full screen on rollover, and flagged that some thumbnails are slightly off-frame and can't be selected — asked whether a "scroll right hot spot" exists for the far edges of the thumbnail row. **2026-09-01** — Two separate bugs, both in `products.html`'s shared gallery markup (all 6 products):

1. **Off-frame, unselectable thumbnails (root cause, not cosmetic):** `.product-thumbs` had no `overflow-x` or wrapping set, so on products with more thumbnails than fit the row (the Trading Card Display Rack has 9), the extras were pushed outside the box and clipped by the parent `.product-gallery`'s `overflow: hidden` — genuinely unclickable, not just visually tight. Fixed by making `.product-thumbs` horizontally scrollable (`overflow-x: auto`) and adding hover-triggered scroll "hot spots" on the far left/right edges (exactly what was asked about) that auto-scroll the strip while the mouse sits over them, plus a directional arrow indicator that only appears when there's more to scroll to. Verified via headless-browser screenshot at desktop and mobile widths: on the Trading Card product (9 thumbs, previously the worst-clipped) every thumbnail is now reachable and clickable.
2. **Full-screen on rollover:** implemented as click-to-enlarge instead of literal hover. The page already had a complete, styled full-screen lightbox (`#lightbox` + `openLightbox()`/`closeLightbox()`) sitting unused in the code — nothing called it. Wired it to each product's main photo (click opens it full-screen, Escape or the ✕ closes it) and added a "Click to enlarge" hint that fades in on hover plus a zoom cursor, so hovering surfaces the affordance and clicking delivers the full-screen view. Went with click-to-open rather than open-on-hover because a modal that pops open the instant your mouse passes over a photo (and has no equivalent on touch devices, which have no hover) is a rough, hard-to-control interaction — happy to switch to literal hover-open if that's genuinely what you want after trying this. Verified via headless browser: click opens the lightbox with the correct image, Escape and the close button both dismiss it, no console errors.

Commit: ac14c7e.

2026.09.01.1339 - Theo's Toys owner asked for new copy to be added to the site, sent as marketing copy for a "TCG Booster Pack Display Box." **2026-09-01** — This is the existing Trading Card Display Rack product (its own photos already show booster packs, not loose cards — "Card holder with a pack pulled out," "stocked with Magic: The Gathering booster packs"), so the old copy's framing ("individual angled slots... trading cards, character cards, and sleeved cards") never actually matched what's photographed. Replaced the description paragraph and feature bullets on that product block in `products.html` with the submitted copy, reformatted into the site's existing paragraph + checkmark-bullet layout (kept your wording, folded the "Ideal for" retailer list into the closing sentence of the paragraph rather than adding a second paragraph, since no other product section has one). Also updated the small category tag from "Collectible Display" to "Retail Display Box" to match the new framing. **Left unchanged:** the `<h3>` title ("Trading Card Display Rack") and the nav/landing-tile label — the submitted copy's own title ("TCG Booster Pack Display Box") would mean renaming the product across nav, the landing grid, and anchors, which felt like a bigger call than "put this copy on the site." Say the word if you want the product renamed to match.

Commit: ac14c7e.

2026.09.01.1045 - Make "Rotate in 3D" the default view on every product gallery (reasoning: the floating 3D model over the blueprint backdrop is the look you want people to land on). **2026-09-01** — The Glasses + Contact Lens Holder gallery already defaulted to 3D; extended that same initial state to the other five products (Trading Card Display Rack, Chilewich Coaster Holder, Hunter Douglas Remote Mount, Steak Knife Holder, ZBiotic Six-Pack Gift Carrier) in `products.html` — flipped the initial `active` class on the "Rotate in 3D" toggle button (and removed it from "Photo"), added `active` to the `model-viewer`, added `is-3d-hidden` to the fallback photo `<img>`, and set the drag-hint's initial opacity to 1, so each gallery now loads straight into the interactive model instead of requiring a click. No JS changes needed — `toggle3D()` already just flips these same classes on click. Verified visually via headless-browser screenshots across all six products, alongside your own `.product-gallery` background tweak (now full-size, right-anchored) that shows more of the drafting graphic behind the floating model. Commit: 134fcc9.

2026.09.01.0937 - Document the DXF→SVG blueprint conversion script (flagged AI Generated) and generate the missing knife-holder and Hunter Douglas mount blueprint backgrounds. **2026-09-01** — The ezdxf-based script that turns a Shapr3D DXF export into the faint tan drafting-style SVG behind each product gallery was never committed (only its `*-blueprint.svg` output was, per the note in the prior Done entry below). Reverse-engineered it and committed it as `tools/dxf_to_blueprint_svg.py` — a header comment on the file flags it **AI Generated**, as requested, so it's clear this was reconstructed rather than the original hand-written script. It works by rendering the DXF modelspace with ezdxf's drawing add-on (`RenderContext` + `Frontend` + `SVGBackend`), forcing `ColorPolicy.CUSTOM` with foreground `#d3c8ac` and `BackgroundPolicy.OFF` for a transparent background, auto-fit to a `Page(0, 0, mm)`. Before trusting the reconstruction on new files, verified it against a DXF that already has a committed SVG: ran it on `images/coasters/chilewich-round.dxf` and confirmed the output matched the existing `chilewich-round-blueprint.svg` structurally (same 400mm×287.2mm page/viewBox, same 407-path count, same transparent background rect) — the only difference was sub-unit coordinate rounding from a newer `ezdxf` version, not a logic mismatch. Then generated the two products that were flagged as still needing theirs: `images/knives-holder/knife-holder-blueprint.svg` (from your `knife-holder.dxf`) and `images/hunter-douglass/hd-3m-claw-blueprint.svg` (from your `hd-3m-claw.dxf`), and wired both into `products.html` the same way the other four are wired in — all 6 products now carry the blueprint watermark. Verified visually via headless-browser screenshots before releasing. **Flag:** `images/knives-holder/knife-holder-dimsdxf` (no file extension — looks like a typo for `knife-holder-dims.dxf`) is a second, different DXF sitting next to `knife-holder.dxf`; left it alone since it's unclear which one is canonical and nothing on the site references it. Commit: e61ba64.

2026.08.31.0700 - Move Card Holder to the top of the products page. **2026-09-01** — Moved the Trading Card Display Rack (the "Card Holder") to the first slot in both the 2×3 landing grid and the detail-section order beneath it on `products.html`, re-alternating the landing-tile background tones and renumbering the `<!-- Product N -->` comments so they still read top-to-bottom in the new order. Commit: e61ba64.

2026.08.31.0700 - Check the updated glasses/contact-lens copy on the Products page for typos and continuity of the dictated story. **2026-09-01** — Checked the paragraph and its feature-list bullets against your dictated text: the prescription count checks out (two in the left eye + one in the right = three), "about fifteen days per prescription (three strips)" in the paragraph matches "about 15 days (three 5-day strips)" in the bullet below it, and "Choose the original 3-Lens version above" correctly refers to the variant chips sitting just above the paragraph in the layout. Also scanned the rest of the page for common misspellings — none found. No changes needed.

2026.08.31.1900 - Blueprint background image cut off by thumbnails on product pages. **2026-09-01** — Root cause: the drafting-graphic background was sized with `background-size: contain`, which scales it to fill the *whole* gallery box; on desktop the CSS Grid stretched that box tall (plenty of hidden slack below the thumbnails), but on mobile (<=900px, single-column layout) the box is only as tall as its own content, leaving almost no room — so no amount of repositioning within the old box could clear the thumbnails on mobile. Fixed by switching to a fixed background-size (320px wide, auto height) bottom-anchored 20px above the box edge, and giving `.product-gallery` a fixed 260px bottom padding so the graphic always has room below the photo + thumbnail strip on every breakpoint, without depending on their exact heights. Verified via headless-browser screenshots at both desktop and mobile widths across all four products that use this background (glasses, coasters, gaming card display, zbiotic six-pack). Commit: 388d1e6.

2026.08.31.1618 - Contact form spam-flag report + follow-up corrections from the previous build. **2026-08-31** — Form fix confirmed working: your test submission's final email did land (Microsoft's junk filter flagged the first one only because formsubmit.co/mail.safenote.co is a new sending domain to your inbox with no reputation yet — nothing wrong on our end, and not something fixable from the site's HTML/DNS since we don't control FormSubmit's sending domain). Marking the sender "not junk" once should train the filter going forward. Also added `_replyto` to the form payload so hitting Reply on the notification email goes straight to the visitor instead of to FormSubmit.

Applied your corrections: swapped Hunter Douglas's landing tile, product-detail default photo, and thumbnail order to lead with `hunter-douglas-2-gang-ref.jpeg` as the real installed print (2-Gang is now the default selected style; updated the on-page note to stop hedging it as a "reference photo"). Swapped the Trading Card Display Rack's landing tile and product-detail default to `gaming-card-display-3.jpg` (the game-shop counter shot), with the card-holder studio photos and your refreshed `gaming-card-display-5.jpg` (multi-holder retail shelf, replacing the old awkward top-down shot) folded into the thumbnail strip. DXF files confirmed staying local/uncommitted per your call on possibly selling the design files later. Gave `lab-notes/index.html` a Build-Log-style page header: a dark banner with eyebrow label, big serif title, and the same drafting-dimension SVG graphic recolored to the site's rust accent instead of Build Log's navy/gold, sitting above the (unchanged) narrow article-width post list.

**Agentic AI approach to reviewing/improving web pages — options, since you asked:**

1.  **Recommended: scheduled audit agent that proposes, never publishes.** A weekly (or after-every-deploy) scheduled Claude session crawls the live site, screenshots every page, checks for console errors/broken links/broken images, and diffs screenshots against the last run to catch unintended visual regressions. Findings get written as new `CHANGES.md` Open items — same workflow you're already using — so you stay the approval gate on anything it finds, rather than it silently "fixing" things. Lowest risk of the three, fits the process you've already built.
2.  **CI checks on every deploy.** Add a GitHub Actions step after the Cloudflare Pages deploy running Lighthouse CI (performance/accessibility/SEO scores) plus a link-checker and HTML validator, failing the build or posting a comment on real regressions. More "always-on" and catches things before they're even live, but more upfront engineering and prone to noisy false positives that need tuning.
3.  **On-demand review, manually triggered.** Exactly what happened in this session — ask for a "critical eyed developer" pass before a bigger publish. Zero standing infrastructure, but only happens when you remember to ask for it.

Recommendation is #1: it matches the CHANGES.md-as-approval-gate pattern already in place, and keeps you deciding what actually ships instead of an agent publishing on its own judgment. Say the word if you want it scheduled. Commit: 935f795.

2025.08.1500 - Canonical README description update. **2026-08-31** — Swapped in your canonical intro paragraph (fixed "Develoment" typo -> "Development") and synced the rest of README.md's Structure section to the real multi-page layout (index.html/products.html/build-log.html/lab-notes/CHANGES.md, plus the images/profile/ folder). Commit: a69f860.

2026.08.31.1443 - Nav unification, Gallery/Lab Journey reorg, real product photos, live contact form. **2026-08-31** — Replaced the inconsistent nav (stale build-log.html links, separate navy/gold branding, no dropdown) with one unified nav on every page: Home (About/Gallery/How It's Made, behaves like the logo), Products (unchanged 6-item dropdown), Lab Journey (Lab Notes/Build Log), plus a single "Get in Touch" CTA — standalone "Contact" link removed. Each dropdown has its own warm accent color (Home=rust, Products=ochre, Lab Journey=clay) on a shared soft/warm palette. Moved Gallery from products.html to index.html between About and the custom-order CTA; added a new Lab Journey section (your printer photo + links to Lab Notes/Build Log) between the custom CTA and Contact. Reviewed images/ subdirectories: swapped the Products landing grid's glasses and trading-card tiles from 3D renders to real studio photos, added the new card-holder photos as product-detail thumbnails, and fixed a sideways EXIF-rotated real photo (zbiotic six-pack). Contact form now really sends to hello@littledevlab.com via FormSubmit (no account signup needed - just click the one-time confirmation email FormSubmit sends you on the very first real submission), with a mailto fallback if the request ever fails. **Flag:** Hunter Douglas mount still has no genuine finished-print photo in the repo (only renders and third-party reference shots) — landing tile uses the best available render until a real photo exists. Also flagging some new untracked files in images/ (several .dxf CAD source files, a hunter-douglas-1-gang-dimensions.md, and gaming-card-display/bulk-display.jpg) that I left uncommitted since they're not referenced by the site and it wasn't clear you want raw CAD source files in the public repo — say the word if you want those added too. Commit: a69f860.

2026.08.30.1530 / 2026.09.30.1545 - Move Products to its own page; nav dropdown of all 6 products; 2x3 landing grid with alternating tile backgrounds; dimensions/blueprint image shifted below the product photo. **2026-08-31** — Built `products.html` (all 6 product-block sections + Gallery moved over), reordered `index.html` to Hero → How It's Made → About → Custom → Contact, added the nav dropdown (desktop hover + mobile expand) on both pages, added the 2x3 landing grid at the top of Products, and shifted `.product-gallery`'s background-position so the blueprint sits below the photo instead of behind it. All internal links (nav, mobile nav, footer, hero CTA) repointed accordingly. Commit: afae4ac.

2026.08.30.1650 - Prompt/Context Engineering Lab Notes post. **2026-08-31** — Drafted at `lab-notes/prompt-and-context-engineering.html` using your opening reflection and all 9 subheadings; each section has a `[NEEDS YOUR EXAMPLE]` placeholder since no real examples were supplied yet (went with option (b) from the proposed solution). Added "Lab Notes" to site nav/footer on both pages. Commit: afae4ac. **Update 2026-08-31:** filled all 9 placeholders with real, redacted examples pulled from other sessions (ServiceNow MCP build, a deploy-config bug, contact lens holder copy, this site's own credential rotation). Commit: 72bc1bc, pushed.

2026.08.31.0800 - SA hands-on-labs / NotebookLM Lab Notes post. **2026-08-31** — Drafted at `lab-notes/hands-on-labs-agentic-ai.html`, published truncated at the hallucination/curation point with a "more on the cleaning pipeline soon" callout, since the missing pipeline steps weren't supplied. Commit: afae4ac.